apiVersion: v1
kind: Service
metadata:
  name: {{.ServiceName}}
  namespace: {{.Namespace}}
  annotations:
    meshless.global/export: "true"
spec:
  type: LoadBalancer
  {{- if .SelectorLabels}}
  selector:
    {{.SelectorLabels}}
  {{- end}}
  ports:
  - port: 80
    targetPort: 80
